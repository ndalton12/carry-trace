name: goal2_paper
seed: 20260601
base: 10
output_dir: data/generated
write_parquet: false
schema_version: goal2.v1
splits:
  train_probe:
    examples_per_slice_per_length: 32
  test_probe:
    examples_per_slice_per_length: 8
digit_lengths: [2, 4, 6]
slices:
  - random
prompt_modes:
  - answer_only
  - free_cot
digit_formats:
  - standard
  - delimited
answer_formats:
  - standard
  - lsd
digit_delimiter: "|"
random_sampling:
  balance_carry_count: true
