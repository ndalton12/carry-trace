name: goal1_paper_like
seed: 20260601
base: 10
output_dir: data/generated
write_parquet: true
schema_version: goal1.v1
splits:
  debug:
    examples_per_slice_per_length: 4
  train_probe:
    examples_per_slice_per_length: 32
  test_probe:
    examples_per_slice_per_length: 8
  test_behavior:
    examples_per_slice_per_length: 32
digit_lengths: [2, 3, 4, 5, 8, 12, 16, 20]
slices:
  - no_carry
  - isolated_carry
  - long_carry_chain
  - carry_distractor
  - random
prompt_modes:
  - answer_only
  - free_cot
digit_formats:
  - plain
  - delimited
answer_formats:
  - standard
  - lsd
digit_delimiter: "|"
